<!-- Paths are relative to the value of `tool_data_path` in galaxy.ini -->
<tables>
    <!-- Locations of all fasta files under genome directory -->
    <table name="all_fasta" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="all_fasta.loc" />
    </table>
    <!-- Locations of dbkeys and len files under genome directory -->
    <table name="__dbkeys__" comment_char="#">
        <columns>value, name, len_path</columns>
        <file path="dbkeys.loc" />
    </table>
    <!-- Locations of fasta indexes generated by samtools faidx -->
    <table name="fasta_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="fasta_indexes.loc" />
    </table>
    <!-- Locations of indexes in the BWA mapper format for BWA versions 0.6 and higher including BWA MEM and ALN-->
    <table name="bwa_mem_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="bwa_mem_index.loc" />
    </table>
    <!-- Locations of STAR indexes -->
    <table name="rnastar_index2x_versioned" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path, with_gene_model, version</columns>
        <file path="rnastar_index2x_versioned.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie2 mapper format -->
    <table name="bowtie2_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="bowtie2_indices.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie2 mapper format for TopHat2 to use -->
    <table name="tophat2_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="tophat2_indices.loc" />
    </table>
    <!-- Built-in homer promoter and ID DBs (dir of <env>/share/data, 1 per organism) -->
    <table name="homer_promoters" comment_char="#">
        <columns>value, dbkey, organism, path, organism_version, promoter_version</columns>
        <file path="homer_promoters.loc"/>
    </table>
    <!-- Locations of preparsed genomes -->
    <table name="homer_preparse" comment_char="#">
        <columns>value, dbkey, mask, size, name, path, path_fasta, version</columns>
        <file path="homer_preparse.loc"/>
    </table>
</tables>